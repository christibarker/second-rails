<h1>Posts#index</h1>
<p>Find me in app/views/posts/index.html.erb</p>

New Posts
<% @posts.each do |post| %>
	<p><%= post.title %>
	<p><%= post.content %>

	<%= link_to "Post Show", post %>
	<%= link_to post.title, post %>
	<%= link_to "Edit Post", edit_post_path(post) %>
	<%= link_to "Destroy", post, method: :delete %>
		<%# link_to user.full_name, user %>
<% end %>